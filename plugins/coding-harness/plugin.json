{
  "name": "coding-harness",
  "short_display_name": "Coding Harness",
  "version": "1.0.0",
  "description": "Coding Harness — file read/write/edit, search, and shell commands in a sandboxed working directory",
  "author": "sapphire",
  "icon": "🧰",
  "url": "https://sapphireblue.dev",
  "priority": 50,
  "managed_hide": true,
  "capabilities": {
    "tools": ["tools/harness.py"],
    "settings": [
      {
        "key": "working_dir",
        "type": "string",
        "label": "Working Directory",
        "default": "user/sapphire-workspace",
        "help": "All tools open here. Relative paths resolve against the Sapphire root; absolute paths allowed. Created on first use."
      },
      {
        "key": "sandbox",
        "type": "boolean",
        "label": "Sandbox to Working Directory",
        "default": true,
        "help": "Confine file tools and command cwd to the working directory"
      },
      {
        "key": "output_limit",
        "type": "number",
        "label": "Output Limit (chars)",
        "default": 6000,
        "help": "Maximum characters returned by file reads and command output"
      },
      {
        "key": "max_timeout",
        "type": "number",
        "label": "Max Timeout (seconds)",
        "default": 300,
        "help": "Maximum allowed timeout for any command"
      },
      {
        "key": "blacklist",
        "type": "string",
        "widget": "textarea",
        "label": "Command Blacklist",
        "default": "rm -rf /\n--no-preserve-root\nmkfs\ndd if=/dev\n:(){ :|:& };:\n> /dev/sda\nchmod -R 777 /\ninit 0\ninit 6",
        "help": "Blocked substrings for run_command, one per line"
      }
    ]
  }
}
